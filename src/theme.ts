import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#00a3e0',
    },
    secondary: {
      main: '#ef3340',
    },
    info: {
      main: '#41748d',
    },
    common: {
      black: '#000000',
    }
  },
});

export default theme;
